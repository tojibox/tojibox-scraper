{
  "name": "togibox-oracle-contracts",
  "version": "1.0.0",
  "description": "TogiboxOracle smart contract — GIWA (OP-Stack EVM L2)",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:testnet": "hardhat run scripts/deploy.js --network giwaSepolia",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
